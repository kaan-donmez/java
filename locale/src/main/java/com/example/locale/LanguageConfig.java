package com.example.locale;

import java.util.Arrays;
import java.util.List;
import java.util.Locale;

public class LanguageConfig {
    public static final List<Locale> LANGUAGES = Arrays.asList(
            new Locale("en"),
            new Locale("tr"),
            new Locale("es"));
}
